{
  "name": "wet-run",
  "version": "0.9.5",
  "description": "Minimal static server, TAP test runner and conventional release flow",
  "repository": "luwes/wet-run",
  "author": "Wesley Luyten <me@wesleyluyten.com> (https://wesleyluyten.com)",
  "license": "MIT",
  "homepage": "https://github.com/luwes/wet-run#readme",
  "bugs": {
    "url": "https://github.com/luwes/wet-run/issues"
  },
  "type": "module",
  "main": "src/index.js",
  "bin": {
    "wet": "src/cli.js"
  },
  "files": [
    "src",
    ".eslintrc.json"
  ],
  "scripts": {
    "clean": "rm -rf coverage",
    "lint": "npx eslint src/*.js -c .eslintrc.json",
    "test": "npm run test:runner && npm run test:server",
    "test:runner": "npm run clean && node src/cli run --coverage",
    "test:server": "npx -y c8@8.0.1 --all --src src --exclude 'test/**' node --test test/*.test.js",
    "coverage": "npx -y c8@8.0.1 report --reporter=text-lcov > ./coverage/lcov.info",
    "dev": "node --watch --no-warnings src/cli"
  },
  "dependencies": {
    "@hono/node-server": "^1.3.3",
    "hono": "^3.11.7",
    "playwright-core": "^1.40.1",
    "ws": "^8.15.1"
  },
  "engines": {
    "node": ">=18.3.0"
  }
}
